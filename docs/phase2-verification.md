# 二期实现与验证记录

日期：2026-09-09。后端基线060e33a，实施分支codex/backend-phase2；前端提交bda63af02b47c83ae1473ae750646d03a64399bd。云环境本次没有执行DDL/DML、发布或图片上传。

## 自动检查

| 范围 | 实测 |
| --- | --- |
| Maven clean verify | 19项、0失败、0跳过，生成可运行JAR；JDK25编译目标Java21 |
| 原用户识别/名片 | 2项回归，身份验证、隔离、保存恢复继续通过 |
| 管理登录安全 | 5项：CSRF、会话轮换、跨实例持久会话、过期、限流、模式隔离、入口重定向与静态图片 |
| 内容只读HTTP | 1项：公开首页及错误查询，无需登记用户 |
| 内容CRUD/迁入兼容 | 10项，真实本地MySQL8.4.10：来源保留、分类/组合筛选、版本409、发布状态/时间/审计、旧tone/null、全部迁入记录、JSON413和缺失multipart400 |
| 图片内容 | 1项：真实PNG识别，空/超限/伪装SVG拒绝；未调用云COS |
| 数据导出 | Node回归通过，资源93/视图119、18条别名、原字段与稳定hash、统计、SQL生成 |
| SQL实际执行 | dry-run、首次、重复跳过、编辑后重复、新hash冲突、NO_BACKSLASH_ESCAPES、两类失败注入回滚均通过 |
| 后台表单 | Node参数/字段/枚举/图片路径/来源/分类覆盖断言通过 |
| 前端 | 后端任务重跑八套Node回归全部通过，来源快照文件SHA一致 |

数据库验证严格限定127.0.0.1:13316/wx_yiyao_test。迁移详细记录见 [local-verification.md](../migration/local-verification.md)。默认未设置CONTENT_TEST_*时，MySQL内容测试跳过；不能把普通CI默认结果误称为真实MySQL验证。

重跑内容检查时，先在隔离测试库执行内容DDL和seed，使用进程环境变量CONTENT_TEST_JDBC_URL、CONTENT_TEST_DB_USER、CONTENT_TEST_DB_PASSWORD，再运行Maven。不要使用生产库。测试会创建随机标记记录并清理，不会删除原表。测试库账号必须能执行所需增量DDL。

## 浏览器与实际HTTP链路

使用本地JAR：管理服务127.0.0.1:18082、mini服务127.0.0.1:18081，共享上述隔离MySQL。仅本地预览关闭Secure cookie，正式默认true。

- 管理账号登录成功；服务重启后已有MySQL会话恢复。
- 首页主推4条，资源管理93条及分页，广告5条，机构8条读取成功；三张机构示意图片可预览。
- 创建仅本地验收资讯 → 草稿保存 → 发布成功 → mini服务详情200且首页推荐含该记录 → 下架 → mini详情404且首页不再出现。未调用云端。
- 广告标题通过网页修改，mini首页接口读取到新标题；随后通过网页恢复原标题，版本递增。
- 原首页主推资源“草莓育种加速器设备开发”在网页保存成功，解决旧price字段阻止编辑的回归。
- 数据概览实际显示13/29/4/0/0/12，与SQL口径一致。
- 原生确认框在内置浏览器间歇阻塞，已去掉可逆发布/下架和已发布保存的多余确认；保留未保存输入离开保护。
- 浏览器检查后只清理本次新建的验收资讯及其关联测试审计，保留源资源及私人表。对旧广告/主推的本地回归编辑保留版本/审计，正式seed文件未受影响。

完成前只读复核发现并修复政策类别丢失、来源覆盖、Spring代理跨对象字段访问、旧字段兼容、后台操作记录参数和主推搜索等问题；回归断言保留在仓库。

## 前端交接

实际代码在D:/codex_work/Wxcxk/yiyaodemo，“云开发路线”任务负责改动。运行期业务种子移到tests/fixtures，无网络失败时的种子回退；原项目私有配置及.codex-transaction未纳入提交。

[前端完整交接及截图](../../yiyaodemo/docs/phase2-frontend-handoff.md)记录了：
- 全部10个内容适配器、5个audience字典/列表/详情和首页无audience详情真实调用本地Java通过。
- 375 CSS px模拟器局部WXML/WXSS编译通过；主推返回、企业分页/分类、政策/机构详情、广告0/1/5张与拖动、刷新失败保留内容通过。
- 模拟器成功流程使用临时响应；最终已恢复真实callContainer，真实云端当前404，页面显示错误重试、没有虚拟统计。
- 以上不等同真机、完整上传包编译或云端联调完成。

## 仍需执行的上线步骤

1. 用户按 [迁移说明](../migration/README.md) 在云yiyao库导入SQL、核对批次和条数。
2. 发布mini和admin两种服务模式，配置后台账号与HTTPS；保持原微信身份私有入口边界。
3. 配置COS公开内容图片上传与展示域名；三张初始图目前仍为内置示意素材。
4. 小程序使用原callContainer环境/服务进行云端和真机验收后再发布。

未验证：Docker镜像实际构建、云数据库版本/权限/备份、线上部署、真实COS上传、手机微信身份与广告原生手势。没有将任何密码、AppSecret或本地测试凭据写入SQL包、Git或前端。

最终浏览器补验：操作记录页正常加载，保留操作人/类型/内容/版本；首页主推搜索“草莓”返回1条对应资源。首页主推与广告的最后修改已通过实际网页保存；最后Java完整检查仍为19/19。
